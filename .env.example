# Copy to .env and fill in to enable AI runbook generation.
# Without this key, ADWatchdog Lite falls back to a deterministic offline
# template so the demo still works end-to-end.
ANTHROPIC_API_KEY=
